import java.util.*;

class Solution {
    public double myPow(double x, int n) {
        // double ans=0;
        // if (n >= 0) {
        //     ans = Math.pow(x, n);
        // }
        // double anss=0;
        // if(n<1){
        // double sign=Math.abs(n);
        // ans=1/(Math.pow(x,n));
        // }
        return Math.pow(x,n);
    }
}
